// Matches Altera Studio's own accent color exactly (the desktop app's
// Configure-window antd theme colorPrimary) -- the site's old shade was
// already close, this makes it identical so the site reads as the same
// product, not a similarly-colored template.
export const COLOR_PRIMARY = "#FE4D41";
export const COLOR_PRIMARY_LIGHT = "#FB766E";
export const COLOR_TEXT_MUTED = "#666";
